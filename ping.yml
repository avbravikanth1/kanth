---
 - hosts: all
   tasks:
     - name: Copy the file from jenkins to mwiapp02
       copy: src= /var/lib/jenkins/workspace/Ravikanth/'Test Ping'  dest=/devapps/devrabbit/nodejsApps
       when: "{{ inventory_hostname == 'appserver' }}"
       
...